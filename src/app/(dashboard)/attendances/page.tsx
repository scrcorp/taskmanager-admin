"use client";

/**
 * Attendance Daily 페이지 — 목업 리디자인 이관.
 * 5 stat cards + status filter tabs + table with anomalies.
 */

import { AttendancePage } from "@/components/schedules/redesign/AttendancePage";

export default function AttendancesDailyPage() {
  return <AttendancePage />;
}
