"use client";

/**
 * Attendance Weekly Summary 페이지 — 목업 리디자인 이관.
 * 4 stat cards + Employees × Days grid + CSV Export.
 */

import { AttendanceSummaryPage } from "@/components/schedules/redesign/AttendanceSummaryPage";

export default function AttendancesSummaryPage() {
  return <AttendanceSummaryPage />;
}
