/**
 * 스케줄 Calendar 페이지 — 목업 리디자인 이관.
 *
 * Weekly/Daily 토글, store selector, filter bar, schedule blocks,
 * context menu, modals (edit/swap/history/confirm/legend) 포함.
 *
 * 데이터는 현재 mock data 사용 (Task 8d에서 React Query로 교체 예정).
 */

import SchedulesCalendarView from "@/components/schedules/redesign/SchedulesCalendarView";

export default function SchedulesPage() {
  return <SchedulesCalendarView />;
}
